#!/usr/bin/env bash
###########################################################
declare   "dir=${HOME}/.meteor"
declare   "url=https://install.meteor.com/"
###########################################################
[[ -d ${dir} ]] &&                                        \
echo      "${dir} already exist"
###########################################################
[[ -d ${dir} ]] ||                                        \
eval      "curl ${url}|sh"
###########################################################
declare   "dir=${HOME}/${1?}"
###########################################################
source    "moun.sh"  "devs"
source    "dir1.sh"  "${dir}"  "${dire[devs]}"
###########################################################
eval      "meteor create --bare ${dir}"
cd        "${dir}"
###########################################################
