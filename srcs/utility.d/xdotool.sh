#!/usr/bin/env bash
###########################################################
declare   "name=$(basename ${BASH_SOURCE[0]%.*})"
declare   "id=$(${name} getwindowfocus)"
eval      "${name} window${1} ${id} ${@:2}"
###########################################################
[[ ${1} == "move" && ! ${4} ]] && eval "xdotool.sh size 50% 92%"
###########################################################
[[ ${1} == "size" && ${2} == "100%" ]] && eval "xdotool.sh move 0 0 1"
###########################################################
