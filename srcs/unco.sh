#!/usr/bin/env bash
####################################################
while read item
do
  declare "bin=$(sed -e /${item}/ s/^#*// -i ${1?})"
          ${bin}
done <    "${2?}"
####################################################
