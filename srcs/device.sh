#!/usr/bin/env bash
#################################
declare   "bin=blkid"
declare   "id=$(${bin} -L ${1?})"
#################################
declare   "bin=gio"
#################################
eval      "${bin} mount -d ${id}"
#################################
