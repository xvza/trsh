#!/usr/bin/env bash
################################
declare   "s0=device label"
declare   "id=blkid -L ${1?$s0}"
################################
[[ -n "$(${id})" ]]            \
|| return ${?}
################################
eval      "gio mount -d ${id}"
################################
