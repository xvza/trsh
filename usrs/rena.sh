#!/usr/bin/env bash
##################################
function      ap()               {
##################################
  for f in "${2-./}"*
  do
    declare   "f=$(basename ${f})"
    mv        ${f} "${f}${1?}"
  done
##################################
                                 }
##################################
function      pr()               {
##################################
  for f in "${2-./}"*
  do
    declare   "f=$(basename ${f})"
    mv        ${f} "${1?}${f}"
  done
##################################
                                 }
##################################
